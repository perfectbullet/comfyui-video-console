# comfyui-video-console 部署

纯静态前端。发版就是本地打 `dist`，丢到服务器对应目录。

| | |
|---|---|
| 线上 | https://ailabs.zenking.cc/comfyui-video-console/ |
| 内网 | http://192.168.8.231:8600/ |
| 服务器目录（示例） | `/data/aigc/comfyui-h3/comfyui-video-console/` |

## 发版

```bash
cd /Users/dxm/Desktop/eureka_work/comfyui-video-console
npm run build
```

产物在 `dist/`。把整个目录内容同步到服务器静态托管路径即可，例如：

```bash
rsync -az --delete dist/ zenking@192.168.8.231:/data/aigc/comfyui-h3/comfyui-video-console/
```

或打包丢过去：

```bash
cd dist && zip -r ../dist.zip .
# 服务器上 unzip 到上述目录
```

刷新页面（必要时强刷）即可。

> `vite.config.js` 里 `base` 用相对路径 `'./'`，同一份 dist 可挂子路径或根路径。不要改成 `'/'`。

## nginx（只配一次）

挂在 `ailabs.zenking.cc` 的同一个 `server` 块里。`proxy_pass` 末尾的 `/` 不要省（用来剥前缀）。

```nginx
# ① 前端静态（上游是托管 dist 的服务，例：172.17.220.95:8600）
location /comfyui-video-console/ {
    proxy_pass http://172.17.220.95:8600/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    client_max_body_size 200m;
    proxy_read_timeout 600s;
    proxy_buffering off;
}

# ② api 接口（缺了则服务器列表拿到 HTML）
location /comfyui-video-console/api/ {
    proxy_pass http://172.17.220.95:8600:8610/api/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    client_max_body_size 200m;
    proxy_read_timeout 600s;
    proxy_buffering off;
}


